This is a simple bot that can interact with user in Discord.

To run the script yourself:

1) Install Node.js.

2) Type into a command line or terminal "npm --save discord.js".

3) replace the token in the login with your bot's secret token.